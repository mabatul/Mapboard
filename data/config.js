/**
 * Map configuration for the test. Pass it through to <interact-map> as-is.
 * If the access token still looks like a placeholder, ask your interviewer.
 */
export const MAP_CONFIG = {
  accessToken:
    '<YOUR_MAPBOX_ACCESS_TOKEN>', // For more information, see https://docs.mapbox.com/help/dive-deeper/access-tokens/
  center: [-58.385, -34.6], // Buenos Aires — [longitude, latitude]
  zoom: 12.5,
};

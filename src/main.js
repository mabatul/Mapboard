/**
 * Your code starts here.
 *
 * Task: define the <geo-comments-app> custom element — see candidate-brief.md.
 *
 * Suggested structure (feel free to change it):
 *   src/components/geoCommentsApp.js   → the host component
 *   src/services/commentsApi.js        → API access
 *
 * Useful imports:
 *   import { MAP_CONFIG } from '../data/config.js';
 *   import { LOCATIONS } from '../data/locations.js';
 *
 * The two widgets are already loaded and registered by index.html:
 *   <interact-map>          → contract in libs/interact-map/README.md
 *   <mock-comments-panel>   → contract in libs/comments-panel/README.md
 */

